---
title: Themes
description: |
  Presenting data by constituency for a variety of themes.
---

<p>{{ description }}</p>

<ul class="grid-list">
{% for theme, data in themes %}
	<li>{% if data['draft'] %}<div class="b5-bg">{% else %}<a href="{{ data['url'] }}" class="{{ data['class'] }}">{% endif %}{{ data['icon'] | safe }}<h2>{{ data['title'] }}</h2><p>{{ data['description'] }}</p>{% if data['draft'] %}</div>{% else %}</a>{% endif %}</li>
{% endfor %}
</ul>
